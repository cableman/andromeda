Drupal module to filter the context module's drag & drop selection menu in
admin 2.x. This modules uses the context module's preprocessor functions
to limit the modules listed in the drop down.

-- Configuration
Go to admin/build/context/filter and limit the modules, that should be shown in the
context drop down menu.
